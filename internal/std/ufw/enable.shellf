def enable() as root {
    guard {
        r = shell { ufw status | grep -q "Status: active" }
        if r { return ok.already }
    }
    apply {
        r = shell { ufw --force enable }
        if !r { return err.runtime(r) }
        return ok.enabled
    }
}
