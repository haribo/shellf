def network(name: str) {
    guard {
        r = shell { docker network inspect "$name" }
        if r.ok { return ok.already }
    }
    apply {
        r = shell { docker network create "$name" }
        if r.exit != 0 { return err.runtime(r) }
    }
    return ok.created
}
