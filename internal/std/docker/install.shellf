def install() {
    guard: shell { command -v docker } -> ok.already when ok

    apply {
        r = shell { curl -fsSL https://get.docker.com | sh }
        when r.exit != 0 -> err.runtime(r)
    }

    ok.installed
}
