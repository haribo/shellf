def install() {
    guard {
        r = shell { command -v docker }
        if r.ok { return ok.already }
    }
    apply {
        r = shell { curl -fsSL https://get.docker.com | sh }
        if r.exit != 0 { return err.runtime(r) }
        return ok.installed
    }
}
