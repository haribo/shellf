def install() as root {
    observe { return state(installed: shell { command -v docker }.exit == 0) }
    apply {
        r = shell { curl -fsSL https://get.docker.com | sh }
        if !r { return err.runtime(r) }
        return ok.installed
    }
}
