def compose-up(dir: str) as root {
    apply {
        r = shell {
            cd "$dir"
            docker compose up -d
        }
        if !r { return err.runtime(r) }
        return ok.up
    }
}
