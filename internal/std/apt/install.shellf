def install(pkg: str) {
    pre-check: when pkg == "" -> err.pkgMustNotBeNull

    guard: shell { dpkg -s "$pkg" } -> ok.alreadyInstalled when ok

    apply {
        r = shell { apt-get install -y "$pkg" }
        when r.exit != 0 -> err.runtime(r)
    }

    ok.installed
}
