# Rendered by shellf per host. shellf variables are @{...}; docker-compose's own
# ${...} interpolation is left untouched (ADR-0021).
DOMAIN=@{domain}
DB_PASSWORD=@{db_password}
# compose reads ${DOMAIN} / ${DB_PASSWORD} below — passed through verbatim
VIRTUAL_HOST=${DOMAIN}
